services:
  bot:
    build: .
    container_name: tg-voice-stt-bot
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    env_file:
      - .env
    environment:
      - PYTHONDONTWRITEBYTECODE=1
